#include <stdio.h>

int main()
{

    /* função para converter string */

    // for (i = 0; Str1[i]!='\0'; i++)
    // {
    // 	if(Str1[i] >= 'A' && Str1[i] <= 'Z')
    // 	{
    // 		Str1[i] = Str1[i] + 32;
    // 	}
    // }
    return 0;
}